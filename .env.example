# Environment Variables Template for SoftwarePros Website
# Copy this file to .env.local for development or configure these in your hosting environment

# Email Configuration - Choose ONE of the following options:

# Option 1: MailerSend SMTP (Recommended)
SMTP_HOST="smtp.mailersend.net"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="MS_vExnkE@test-eqvygm0kpqjl0p7w.mlsender.net"
SMTP_PASS="mssp.ZNBr34Y.3zxk54vpzxq4jy6v.1V2Lb2b"

# Option 2: Custom SMTP Server (Alternative for cPanel)
# Most cPanel providers support SMTP through their mail servers
# SMTP_HOST="mail.yourdomain.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-email@yourdomain.com"
# SMTP_PASS="your-email-password"

# Option 3: Gmail SMTP (Alternative)
# Note: Use App Password, not regular password
# GMAIL_USER="your-gmail@gmail.com"
# GMAIL_APP_PASSWORD="your-16-character-app-password"

# Option 4: cPanel Localhost SMTP (Fallback)
# Most cPanel servers have Exim running on localhost
# No configuration needed - this is automatic fallback

# Contact Form Settings
CONTACT_FROM_EMAIL="noreply@test-eqvygm0kpqjl0p7w.mlsender.net"

# Next.js Configuration
NODE_ENV="production"
NEXT_PUBLIC_SITE_URL="https://yourdomain.com"

# Optional: Custom sendmail path (for advanced users)
# SENDMAIL_PATH="/usr/sbin/sendmail"

# Cloudflare RealtimeKit Configuration
# Get these from your Cloudflare Dashboard at https://dash.cloudflare.com/calls

# Method 1: Use Organization ID + API Key (Basic Auth)
CLOUDFLARE_REALTIME_ORG_ID="your-organization-id"
CLOUDFLARE_REALTIME_API_KEY="your-api-key"

# Method 2: Use pre-generated auth header directly (Alternative)
# CLOUDFLARE_REALTIME_AUTH_HEADER="Bearer your-pre-generated-token"

# API Base URL
CLOUDFLARE_REALTIME_API_URL="https://api.realtime.cloudflare.com/v2"